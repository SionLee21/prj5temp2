<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Board">

    <insert id="insertBoard">
        <![CDATA[
        INSERT INTO Board (productName, price, category, nickname, location, description, seller_id, image_url, registration_date, product_condition)
        VALUES (#{productName}, #{price}, #{category}, #{nickname} #{location}, #{description}, #{sellerId}, #{imageUrl}, #{registrationDate}, #{condition})
        ]]>
    </insert>

    <update id="updateBoard">
        <![CDATA[
        UPDATE Board
        SET productName=#{productName}, price=#{price}, category=#{category}, location=#{location},
            description=#{description}, seller_id=#{sellerId}, image_url=#{imageUrl},
            registration_date=#{registrationDate}, product_condition=#{condition}
        WHERE seq=#{seq}
        ]]>
    </update>

    <delete id="deleteBoard">
        <![CDATA[
        DELETE FROM Board
        WHERE seq=#{seq}
        ]]>
    </delete>

    <select id="getBoard" resultType="board">
        <![CDATA[
        SELECT * FROM Board WHERE seq=#{seq}
        ]]>
    </select>

    <select id="getBoardList" resultType="board">
        <![CDATA[
        SELECT * FROM Board ORDER BY seq DESC
        ]]>
    </select>

</mapper>
